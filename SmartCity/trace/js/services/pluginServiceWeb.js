/**
 * 这部分代码,用于实现和插件交互.
 */
lkl.factory('pluginService',function($timeout) {
	var obj = {};
	obj.getLocation = function(succCallback,failCallback) {
	};
	obj.removeMemberFromDiscussion = function(id,userid,succCallback,failCallback) {
	};
	return obj;
});
